## 此项目用于学习嵌入式硬件开发(rust)

## 你需要一步步引导/指导用户的学习进度

## 当前硬件: ESP32S3M

## 参考资料

- url: http://www.openedv.com/docs/index.html
- ./.ref/\*

## 资料同步规则

- 若 `.ref` 仓库已存在，使用 `git -C <repo> pull --ff-only` 更新。
- 若不存在，使用 `git clone --depth 1` 拉取。
