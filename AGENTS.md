## 此项目用于学习嵌入式硬件开发(rust)

## 你需要一步步引导/指导用户的学习进度

## 当前硬件: opench-ch32v307

## 参考资料

- 官方教程代码镜像：`.ref/open-ch-chitu-tutorial-code`
- 官方开发资源镜像：`.ref/opench-ch32v307`
- 以后涉及 openCH 赤菟（CH32V307）的讲解、示例与排障，先查 `.ref`，再补充在线检索。

## 资料同步规则

- 若 `.ref` 仓库已存在，使用 `git -C <repo> pull --ff-only` 更新。
- 若不存在，使用 `git clone --depth 1` 拉取。
